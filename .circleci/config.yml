version: 2.1

orbs:
  node: circleci/node@4.2.0
  codecov: codecov/codecov@1.1.3

jobs:
  tests:
    docker:
      - image: circleci/node:14.16.0-browsers
    steps:
      - checkout
      - node/install-yarn
      - run: node --version
      - run: yarn --version
      - node/install-packages:
          pkg-manager: yarn
      - run: yarn run lint
      - run: yarn run cover
      - codecov/upload:
          file: coverage/*.json
      - run:
          name: Publish to Chromatic
          command: yarn chromatic --exit-zero-on-changes --project-token=${CHROMATIC_TOKEN}
  versioning:
    docker:
      - image: circleci/node:12.18.3
    steps:
      - checkout
      - run:
          name: Check package version
          command: node scripts/check-version maurolsn web-mauro-leandro

workflows:
  ci:
    jobs:
      - tests
      - versioning:
          filters:
            branches:
              ignore: master
            tags:
              ignore: /.*/
